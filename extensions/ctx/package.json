{
  "name": "pi-ctx",
  "version": "0.1.0",
  "private": true,
  "description": "Inspect Pi's inferred active context from session state and extension snapshots.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}
